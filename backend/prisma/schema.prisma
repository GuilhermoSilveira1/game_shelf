datasource db {
  provider = "postgresql" // ajuste para seu provider
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

model Game {
  id        Int         @id            // id da IGDB como PK
  name      String      @db.VarChar(255)
  summary   String?
  coverUrl  String?
  createdAt DateTime    @default(now())
  updatedAt DateTime    @updatedAt

  genres    GameGenre[]

  @@index([name])
}

model Genre {
  id     Int        @id              // id da IGDB
  name   String     @unique
  slug   String?
  games  GameGenre[]
}

model GameGenre {
  gameId  Int
  genreId Int
  game    Game  @relation(fields: [gameId], references: [id])
  genre   Genre @relation(fields: [genreId], references: [id])

  @@id([gameId, genreId])
}

model Profile {
  id        Int      @id @default(autoincrement())
  name      String
  email     String   @unique
  createdAt DateTime @default(now())
}